{% if isSorted(newSortBy)%}
<a class="btn btn-info btn-sm active" href="{{link}}"><span class="glyphicon glyphicon-menu-{{iconKey}}" aria-hidden="true"></span> {{buttonValue}}</a>
{% else %}
<a class="btn btn-info btn-sm" href="{{link}}">{{buttonValue}}</a>
{% endif %}
