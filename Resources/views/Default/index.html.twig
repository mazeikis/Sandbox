<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<title>{% block title %}{% if title is defined %}{{ title }}{% else %}sandbox|project{% endif %}{% endblock %}</title>
		{% block style %}
		{% spaceless %}
		<link rel="stylesheet" href="{{ asset('css/normalize.css') }}"/>
		<link rel="stylesheet" href="{{ asset('css/bootstrap.css') }}"/>
	    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.2/jquery.min.js"></script>
		<script  type="text/javascript" src="{{ asset('js/bootstrap.min.js') }}"></script>
		{% endspaceless %}
		{% endblock %}
	</head>
<body>
<div class="navbar navbar-inverse navbar-fixed-top" role="navigation">

	<div class="navbar-header">
		<button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
		<span class="sr-only">Toggle navigation</span>
		<span class="icon-bar"></span>
		<span class="icon-bar"></span>
		<span class="icon-bar"></span>
		</button>
		<a class="navbar-brand" rel="home" href="{{path('_home')}}">Code Sandbox</a>
	</div>
	
	<div class="collapse navbar-collapse">
		
		<ul class="nav navbar-nav">
			<li><a href="{{path('_home')}}">Home</a></li>
			<li><a href="{{path('_gallery')}}">Gallery</a></li>
			<li><a href="{{path('_search')}}">Search</a></li>
			<li><a href="{{path('_about')}}">About</a></li>
		</ul>
		<div class="col-sm-3 col-md-7">
		<form class="navbar-form" role="search" action="{{path('_search')}}" method="get">
		<div class="input-group  navbar-right">
			<input type="text" class="form-control" placeholder="{{app.request.query.get('q')|default('Search')}}" name="q">
			<div class="input-group-btn">
				<button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i></button>
			</div>
		</div>
		</form>
		</div>
		<ul class="nav navbar-nav">
			{% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
			<li><a href="{{ path('_user', { slug : app.user.id })}}">{{app.user.username}}</a></li>
			<li class="divider-vertical"></li>
			<li><a href="{{ path('logout') }}">Logout</a></li>
    		{% else %}
			<li><a href="{{path('_registration')}}">Sign Up</a></li>
			<li class="divider-vertical"></li>
			<li class="dropdown">
				<a class="dropdown-toggle" href="#" data-toggle="dropdown">Sign In <strong class="caret"></strong></a>
				<div class="dropdown-menu pull-right" style="padding: 15px; padding-bottom: 0px;">
					<form action="{{ path('login_check')}}" method="post">
						<input style="margin-bottom: 15px;" type="text" id="username" placeholder="Username" {% if last_username is defined %} value="{{ last_username }}" {% endif %} name="_username">
						<input style="margin-bottom: 15px;" type="password" placeholder="Password" id="password" name="_password">
						<input style="float: left; margin-right: 10px;" type="checkbox" name="remember-me" id="remember-me" value="1">
						<label class="string optional" for="user_remember_me"> Remember me</label>
						<input class="btn btn-primary btn-block" style="margin-bottom: 15px;" type="submit" id="sign-in" value="Sign In">
					</form>
				</div>
			</li>
			{% endif %}
		</ul>		
	</div>
</div>
{% block content %}
		<link rel="stylesheet" href="{{ asset('css/heroic-features.css') }}"/>
	   <div class="container">
	   	<br>
        <!-- Jumbotron Header -->
        <header class="jumbotron hero-spacer">
        	{% if app.session.flashBag.has('success') %}
    				<div class="alert alert-success">
			            {% for msg in app.session.flashBag.get('success') %}
			                    	<strong>{{ msg }}</strong>
			            {% endfor %}
        			</div>
    		{% else %}
            <h1>Welcome to the sandbox project!</h1>
            <p>A homemade and Symfony 2 powered image gallery mad by me in spare time. It's still work in progress, so don't worry too much if You break something! ;)</p>
            <p><a class="btn btn-primary btn-large" href="{{path('_gallery')}}">Go to gallery!</a>
            </p>
        {% endif %}
        </header>

        <hr>

        <!-- Title -->
        <div class="row">
            <div class="col-lg-12">
                <h3>Latest Uploads</h3>
            </div>
        </div>
        <!-- /.row -->

        <!-- Page Features -->
        {% if recent is defined %}
        <div class="row text-center">
        	{% for item in recent %}
            <div class="col-md-3 col-sm-6 hero-feature">
                <div class="thumbnail">
	         		<img src='{{ (item.getFileName~'.'~item.getExtension) | imagine_filter('thumb')}}' class="img-responsive">
                    <div class="caption">
                        <h3>{{item.getTitle}}</h3>
                        <p>{{item.getDescription}}</p>
                        <p>
                            <a href='{{path('_image', { 'id' : item.getId })}}' class="btn btn-primary" >Check me out!</a>
                        </p>
                    </div>
                </div>
            </div>
        {% endfor %}
        </div>
        {% endif %}
    </div>
	{% endblock %}
	 <div class="container"><hr></div>
</body>
</html>