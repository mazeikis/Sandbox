<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<title>{% block title %}{% if title is defined %}{{ title }}{% else %}sandbox|project{% endif %}{% endblock %}</title>
		{% block style %}
		{% spaceless %}
		<link rel="stylesheet" href="{{ asset('bundles/main/css/normalize.css') }}"/>
		<link rel="stylesheet" href="{{ asset('bundles/main/css/bootstrap.css') }}"/>
		{% endspaceless %}
		{% endblock %}
	</head>
<body>
	<nav class="navbar navbar-inverse navbar-fixed-top">
		<div class="container">
			<div class="navbar-header">
				<a class="navbar-brand" href="{{path('_home')}}">sandbox</a>
			</div>
			<div id="navbar" class="collapse navbar-collapse">
				<ul class="nav navbar-nav">
					<li class="active"><a href="{{path('_home')}}">Home</a></li>
					<li><a href="{{path('_gallery')}}">galllery</a></li>
					<li><a href="{{path('_about')}}">about</a></li>
				</ul>
			</div>
       		<form class="navbar-form navbar-right" role="search" action="{{path('_search')}}" method="get">
	       		<div class="input-group">
	           		<input type="text" class="form-control" placeholder="Search" name="q">
	           		<div class="input-group-btn">
	           			<button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i></button>
	           		</div>
	        	</div>
        	</form>
			{% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
				<div class="navbar-form navbar-right">
	    			<a class="btn btn-primary btn-small" href="{{ path('_user', { slug : app.user.id })}}">{{app.user.username}}</a><a href="{{ path('logout') }}" class="btn btn-warning btn-small">Logout</a>
	    		</div>
    		{% else %}
				<div class="navbar-form navbar-right">					
					<form class="form-group" action="{{ path('login_check')}}" method="post">
						<input type="text" id="username" placeholder="Username" {% if last_username is defined %}value="{{ last_username }}"{% endif %}name="_username">
						<input type="password" placeholder="Password" id="password" name="_password">
						<button type="submit" class="btn btn-success">Sign in</button>
					</form>
					<form class="form-group" action="{{path('_registration')}}" method="post">
						<button type="submit" class="btn btn-primary" >Register</button>
					</form>
					<a href='{{path('_reset')}}'>Forgot password?</a>
				</div>
			{% endif %}
		</div>
	</nav>
	{% block content %}
	   <div class="container">
	   	<br>
        <!-- Jumbotron Header -->
        <header class="jumbotron hero-spacer">
        	{% if app.session.flashBag.has('success') %}
    				<div class="alert alert-success">
			            {% for msg in app.session.flashBag.get('success') %}
			                    	<strong>{{ msg }}</strong>
			            {% endfor %}
        			</div>
    		{% else %}
            <h1>Welcome to the sandbox project!</h1>
            <p>A homemade and Symfony 2 powered image gallery mad by me in spare time. It's still work in progress, so don't worry too much if You break something! ;)</p>
            <p><a class="btn btn-primary btn-large" href="{{path('_gallery')}}">Go to gallery!</a>
            </p>
        {% endif %}
        </header>

        <hr>

        <!-- Title -->
        <div class="row">
            <div class="col-lg-12">
                <h3>Latest Uploads</h3>
            </div>
        </div>
        <!-- /.row -->

        <!-- Page Features -->
        {% if recent is defined %}
        <div class="row text-center">
        	{% for item in recent %}
            <div class="col-md-3 col-sm-6 hero-feature">
                <div class="thumbnail">
	         		<img src='{{ (item.getFileName~'.'~item.getExtension) | imagine_filter('my_thumb')}}' class="img-thumbnail">
                    <div class="caption">
                        <h3>{{item.getTitle}}</h3>
                        <p>{{item.getDescription}}</p>
                        <p>
                            <a href='{{path('_image', { 'id' : item.getId })}}' class="btn btn-primary" >Check me out!</a>
                        </p>
                    </div>
                </div>
            </div>
        {% endfor %}

        </div>
        <!-- /.row -->
		{% endif %}
        <hr>

        <!-- Footer -->
        <footer>
            <div class="row">
                <div class="col-lg-12">
                    <p>Copyright &copy; sandbox | project 2015</p>
                </div>
            </div>
        </footer>

    </div>
	{% endblock %}
</body>
</html>